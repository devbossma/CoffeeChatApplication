/**
 * Not So Simple Chat — the coffee shop chat application, on Spring Boot.
 *
 * <p>See {@code PRD.md} at the repository root for the full scope, the pattern-by-pattern
 * design mapping, and what's explicitly out of scope. The sub-packages here mirror the
 * assignment's own mandated structure — one package per GoF pattern, plus the usual
 * {@code model}/{@code controller}/{@code service}/{@code repository} layers.
 */
package dev.saberlabs.coffeechat;
