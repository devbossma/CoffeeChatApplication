/**
 * Application services — order handling, chat, notifications, and the barista/queue
 * machinery from Part 02. Orchestrates the domain and pattern packages; called by
 * {@code controller} (usually through {@code facade}), not the other way around.
 */
package dev.saberlabs.coffeechat.service;
