/**
 * Pattern: FACADE.
 *
 * <p>{@code CoffeeShopFacade} is the single entry point controllers call into — it hides the
 * factory/decorator/strategy/command/adapter machinery behind one simple service surface.
 * See {@code PRD.md} section 7.2, row 8.
 */
package dev.saberlabs.coffeechat.facade;
