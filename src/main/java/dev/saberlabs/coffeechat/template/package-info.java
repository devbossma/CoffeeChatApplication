/**
 * Pattern: TEMPLATE METHOD.
 *
 * <p>An abstract preparation skeleton shared by every coffee type, with each type filling in
 * its own steps (brew time, milk, ...) in a subclass. Plain Java here — Spring has no special
 * idiom for Template Method beyond what the language already gives you.
 */
package dev.saberlabs.coffeechat.template;
