# Local profile (mvn spring-boot:run -Dspring-boot.run.profiles=local): seeds the first MANAGER at startup
# so the API can be exercised from an empty database with curl. See docs/API.md.
coffeeshop.bootstrap.manager-name=Manager
